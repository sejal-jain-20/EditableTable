import React from 'react'

const Popup = () => {
  return (
    <div>
      <h3 style={{}}><button onClick={()=>alert("Add/Delete an employee data using below table")}>Click me</button></h3>
    </div>
  )
}

export default Popup;
